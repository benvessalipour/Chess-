#include "Spieler.h"

/*bool MenschSpieler::makeMove(Spielfeld& board, const Position& from, const Position& to) {
    if (board.isMoveValid(from, to)) {
        return board.movePiece(from, to);
    }
    return false;
}*/
